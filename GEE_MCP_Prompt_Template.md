# GEE 数据下载 · 用户提示词模板

> 用法：把下面的模板复制给 AI 助手，按你的需求填空（带 `【】` 的部分），
> 其余照抄即可。AI 会负责登录、选数据集、规划、下载、检查，全程不需要你碰代码。
>
> 本模板基于 MCP `gee_help` 的实际参数清单整理。

---

## 模板一：标准下载（大多数场景用这个）

```
请帮我从 Google Earth Engine 下载一份遥感数据。

【数据】：地表温度（LST）
         （不知道 ID 就写名字，AI 会帮你选；知道就写，如 MODIS/061/MOD11A1）
【时间范围】：2021年1月1日 至 2021年1月9日
【区域】：中国
【空间分辨率】：1 公里（或 250 米 / 30 米）
【时间粒度】：逐日（或 逐月平均 / 逐年平均 / 每天一景原始数据）
【波段】：只要白天地表温度这一个波段（或不限，全部波段）
【输出方式】：每个时间点单独一个文件
             （或：合并成一个多波段文件 stack_periods=true）
【文件大小优化】：可以的话转成 float32 减小体积（dtype=float32）
【输出目录】：D:/GEE_Data
【其他要求】：无

开始前请先确认：
1. 数据集和边界是否可用；
2. 如果任务比较大，先用 dry_run 预览一下要下载多少文件、多大体积、
   预计要多久，告诉我之后我再确认要不要下载。
```

---

## 模板二：长时间序列 / 大范围（分月或分年聚合）

```
请从 Google Earth Engine 下载 【植被指数 NDVI】 数据。

【时间范围】：2018年1月 至 2022年12月（共 5 年）
【区域】：【安徽省】
【空间分辨率】：250 米
【时间粒度】：按月平均（每年 12 个月，共 60 个文件）
              （或：按年平均，共 5 个文件）
【波段】：NDVI 和 EVI 两个波段
【输出方式】：合并成一个多波段文件（stack_periods=true）
【文件大小优化】：dtype=float32
【输出目录】：D:/GEE_Data/ndvi_anhui
【其他要求】：下载完成后告诉我每个文件的路径，并说明数据质量检查结果。
```

---

## 模板三：只要某一天的快照（单景）

```
请从 Google Earth Engine 下载 【2021年6月15日 中国某市】 的影像。

【数据集】：帮我选合适的（Landsat 或 Sentinel-2 均可，优先云量少的）
【时间】：2021年6月15日 前后 30 天内，云量最少的一景
【区域】：【北京市】
【空间分辨率】：30 米（或 10 米）
【波段】：真彩色（红、绿、蓝）
【输出目录】：D:/GEE_Data
【其他要求】：无
```

---

## 模板四：不确定选什么数据集（让 AI 推荐）

```
我想研究 【土壤湿度在华北平原的时空变化】，需要下载 GEE 数据。

我自己不确定用哪个数据集，请：
1. 先帮我对比推荐 2~3 个合适的数据集（说明分辨率、时间范围、优缺点）；
2. 我选定后再按标准流程下载。

时间范围：【2015年 至 2023年】
区域：【华北平原】
空间分辨率：尽量 1 公里以内
时间粒度：逐月平均
输出目录：D:/GEE_Data
```

---

## 必填项（4 个，模板里已用【】标出）

| 空位 | 对应参数 | 怎么填 | 不填会怎样 |
|---|---|---|---|
| 【数据】 | `dataset`（数据集 ID） | 直接填 ID（如 `MODIS/061/MOD11A1`），或写名字让 AI 选（如"地表温度 LST"） | AI 会问你 |
| 【时间范围】 | `start_date` / `end_date` | 起止日期，如 2021-01-01 ~ 2021-01-09 | AI 会问你 |
| 【区域】 | `boundary`（边界） | 省市名 / 流域名 / 任意描述，AI 用边界资产定位 | AI 会问你 |

## 填写说明（其余可选）

| 空位 | 怎么填 | 不填会怎样 |
|---|---|---|
| 【空间分辨率】 | 30 米 / 250 米 / 1 公里等 | 默认 1000 米（1 公里） |
| 【时间粒度】 | 逐日 / 逐月 / 逐年 / 每天一景原始 | 默认逐景原始（native） |
| 【波段】 | 如"只要 NDVI""全部波段" | 下载全部波段 |
| 【输出方式】 | 每个时间点一个文件，或合并成一个多波段文件 | 每个时间点一个文件 |
| 【文件大小优化】 | 写"可以的话转 float32"即可；不写则保留原始精度 | 保留原始 float64 |
| 【输出目录】 | 本机文件夹路径 | 默认 D:/GEE_Data |

---

## 可选参数速查（想精确控制时，用大白话告诉 AI 即可）

> 注意：下面是**可选**参数；**必选**的是数据 ID（`dataset`）、开始/结束日期、
> 边界（`boundary`）——这三个在模板里用【】标出，其余全部可选。

这些就是 MCP 里 `gee_download` 的全部可选参数，AI 会自动对号入座：

| 你想控制什么 | 怎么说 | 默认值 |
|---|---|---|
| 输出目录 | "存到 D:/GEE_Data/xxx" | D:/GEE_Data |
| 分辨率 | "1 公里" / "250 米" / "30 米" | 1000 米 |
| 坐标系 | "用 Web 墨卡托"（EPSG:3857）/"用 WGS84"（EPSG:4326） | EPSG:3857 |
| 波段 | "只要 NDVI 这一个波段" | 全部波段 |
| 时间粒度 | "逐日" / "逐月平均" / "逐年平均" / "每天一景原始" | 逐景原始 |
| 分组聚合方式 | "每月取平均"（也可：中位数/最大值/最小值/求和/镶嵌/首景/最优） | 分组时默认平均 mean |
| 多时间片合并 | "合并成一个多波段文件" | 不合并，每片一个文件 |
| 裁剪 | "只保留边界内的像元"（clip） | 不裁剪，整块矩形输出 |
| 下载方式 | 不写（本地直下，默认）；或明确"用远程导出"（strategy=export） | auto（本地直下） |
| 减小体积 | "转成 float32"（或 int16，适合 LST 等整型语义数据） | 保留 GEE 原始 float64 |
| 只预览 | 说"先预览一下，不要真下载"（dry_run=true） | 直接下载 |
| 输出格式 | 默认 GeoTIFF（一般不用改） | GeoTIFF |
| 任务备注 | "这个任务叫 XX"（description） | 自动生成 |

> 小提示：如果 AI 说"没有 dtype / 减小体积这个参数"，说明你连接的 MCP 服务
> 还是旧版本，需要重启服务进程后才会出现该参数。

---

## 你可能会收到的提问（正常现象，简单回答即可）

- **"数据集 X 与 Y 选哪个？"** → 按我的需求选就行，你定。
- **"这个任务预计 X 个文件、共 Y GB，确认下载吗？"** → 确认 / 让我调整（比如改小分辨率、缩小时间范围、按月聚合）。
- **"建议按月平均而不是逐日，可以减少到 12 个文件，可以吗？"** → 可以 / 不行，我要逐日。
- **"Google Drive 空间不足"（仅当用了远程导出时）** → 清一下 Drive 空间，或让我改用本地直下。
- **"下载中途失败了，正在补传缺失的部分"** → 等它完成即可（支持断点续传，不用重来）。
- **"这个任务本地直下会很慢，建议用远程导出（export），可以吗？"** → 可以 / 不行，保持本地直下。

---

## 一句话速记版（聊天里最快用法）

```
帮我从 GEE 下载【地表温度】，时间【2021-01-01 到 2021-01-09】，
区域【中国】，1 公里，逐日，只要白天 LST 波段，转 float32 减小体积，
输出到 D:/GEE_Data。任务大就先预览给我确认。
```
