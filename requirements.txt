# 核心依赖（与 pyproject.toml 一致）
earthengine-api>=0.1.390
mcp>=2.0.0
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
requests>=2.31
httpx>=0.27
PyYAML>=6.0

# Google Drive 本地回传（可选）
google-api-python-client>=2.120
google-auth>=2.27

# 开发
pytest>=8
